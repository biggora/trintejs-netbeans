/**
 *  ${modelName} schema
 *
 *  @package     ${projectName}
 *  @version     ${version}
 *  @desc        ${description}
 *  @author      ${user}
 *  @created     ${created}
 *
 *  Created by Netbeans Create Model wizard
 *  App based on TrinteJS MVC framework
 *  TrinteJS homepage http://www.trintejs.com
 **/

module.exports = function(schema){
    var ${modelName} = schema.define('${modelName}', {
${fields}
    });
    return ${modelName};
};